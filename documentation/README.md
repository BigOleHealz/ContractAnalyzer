# Test Feature Documentation

This documentation provides information about the newly added test feature.

## Accessing the Test Page

Navigate to the `/test` route in the application to view the test page. This page is accessible via the navigation bar.

## Test Component

The test component is a reusable component that displays a simple message. It is located in `src/components/TestComponent.tsx`.
