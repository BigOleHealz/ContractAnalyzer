{
  "type": "nextjs",
  "version": "1.0.2",
    "test": "pnpm run test",
    "test:watch": "pnpm run test:watch",
  "commands": {
    "build": "pnpm run build",
    "start": "pnpm run database:sync && pnpm run start"
  },
  "commandsWorkspace": {
    "init": "pnpm install && pnpm run global:sync ; pnpm run database:seed",
    "dev": "pnpm run dev",
    "syncModels": "pnpm run global:sync"
  }
}
