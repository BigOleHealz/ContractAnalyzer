## Test Directory

This directory contains all the test cases for the project. The tests are organized to validate the existing codebase and ensure that all components are functioning as expected.

### Structure

- `unit/`: Contains unit tests for individual components.
- `integration/`: Contains integration tests for combined components.

### Running Tests
To run the tests, use the command specified in the project's `package.json` file.