# Testing Framework

This document outlines the testing framework used in this project.

## Automated Testing

The project uses `unittest` for automated testing. All test modules are located in the `scripts` directory and can be executed using the `test_suite.py` script located in the `tests` directory.

To run all tests, execute the `test_suite.py` script.
