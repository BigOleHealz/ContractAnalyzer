# Testing Guide

This document provides guidelines on how to write and run tests for the project.

## Running Tests

To run tests, use the following command:

```
npm test
```

Ensure you have installed all dependencies by running `npm install` before executing the tests.
